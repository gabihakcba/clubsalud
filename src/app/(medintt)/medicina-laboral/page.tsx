import { type ReactElement } from 'react'

export default function OccupationalMedicine({
  children
}: {
  children: ReactElement
}): ReactElement {
  return <div className='h-full w-full p-0 m-0'>{children}</div>
}
